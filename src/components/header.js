const Header = () => {
  const template = `
    <header>
    <h1>Get started quickly with Parcel</h1>
    <p>This is a boilerplate for a vanilla JavaScript workflow</p>
    </header>
  `;

  return template;
};

export default Header;
